# SPA fallback — every path serves the app shell. Bare share-paths like /join are
# bounced into the HashRouter by the early script in index.html (Cloudflare Pages
# _redirects cannot target a URL fragment, so the redirect is done client-side).
/*      /index.html   200
