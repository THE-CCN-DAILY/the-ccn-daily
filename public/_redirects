# Shareable invite link: theccndaily.com/join → the hash-routed join page.
# The app is a HashRouter SPA, so a bare /join path must bounce to /#/join for the
# client router to land on the sign-up page.
/join   /#/join   302

# SPA fallback — every other path serves the app shell.
/*      /index.html   200
