import React from 'react';
import Card from '../Card';

const ReadingPlanReader: React.FC = () => {
  return (
    <Card>
      <h2 className="text-2xl font-bold text-brand-text-primary">Reading Plan Reader</h2>
      <p className="text-brand-text-secondary">A focused space to follow a reading rhythm, mark progress, and return to the next passage without losing the thread.</p>
    </Card>
  );
};

export default ReadingPlanReader;
